Your sign-in link<%= @user.first_name.present? ? ", #{@user.first_name}" : "" %>

Click and you're in. No password needed:
<%= @magic_link_url %>

The link works once and expires in 15 minutes. If you didn't request it,
ignore this email and nothing happens.
