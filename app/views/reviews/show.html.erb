<% content_for :title, "#{@review.user.first_name}'s tasting of #{@review.bottle.name} - Whiskey Share Society" %>

<section class="w-full bg-char px-4 pb-10 pt-24">
  <div class="mx-auto max-w-2xl text-center">
    <p class="eyebrow mb-3 text-whiskey-300/90">A tasting, on the record</p>
    <div class="mb-4 flex justify-center">
      <div class="rounded-2xl bg-cream/95 px-5 py-3">
        <%= render "bottles/placeholder", bottle: @review.bottle, size: "h-14" %>
      </div>
    </div>
    <h1 class="font-display text-3xl font-semibold text-cream sm:text-4xl">
      <%= link_to @review.bottle.name, bottle_path(@review.bottle), class: "transition hover:text-whiskey-200" %>
    </h1>
    <% if @review.bottle.distillery.present? %>
      <p class="mt-2 text-cream/80">
        <%= link_to @review.bottle.distillery, reviews_path(distillery: @review.bottle.distillery),
            class: "underline decoration-cream/30 underline-offset-4 transition hover:text-whiskey-200" %>
      </p>
    <% end %>
    <div class="mt-5 flex items-center justify-center gap-3 text-xl">
      <%= render "bottles/rating", value: @review.rating.to_f %>
      <span class="font-semibold text-cream"><%= number_with_precision(@review.rating, precision: 2, strip_insignificant_zeros: true) %></span>
    </div>
    <p class="mt-3 text-cream/70">
      <%= @review.user.first_name %> <%= @review.user.last_name %> ·
      <%= @review.created_at.strftime("%B %-d, %Y") %>
    </p>
  </div>
</section>

<section class="w-full bg-white px-4 py-12">
  <div class="mx-auto max-w-2xl">
    <% if @review.notes.present? %>
      <p class="whitespace-pre-line text-lg leading-relaxed text-gray-700"><%= @review.notes %></p>
    <% end %>

    <% tasting = { "Nose" => @review.nose, "Palate" => @review.palate, "Finish" => @review.finish, "Body" => @review.body_notes }.compact_blank %>
    <% if tasting.any? %>
      <div class="mt-10 border-t border-gray-100 pt-8">
        <p class="eyebrow text-whiskey-600">The tasting</p>
        <% icons = {
          "Nose"   => "M5 12c3-5 11-5 14 0M7 15c2-3 8-3 10 0M9.5 18c1-1.5 4-1.5 5 0",
          "Palate" => "M12 4c3 4 6 7 6 10.5a6 6 0 1 1-12 0C6 11 9 8 12 4z",
          "Finish" => "M12 8v4l2.5 2.5M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0z",
          "Body"   => "M12 6a7 3 0 1 0 0 .1M5 6v6c0 1.7 3.1 3 7 3s7-1.3 7-3V6M5 12v6c0 1.7 3.1 3 7 3s7-1.3 7-3v-6"
        } %>
        <dl class="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4">
          <% tasting.each do |label, text| %>
            <div class="rounded-xl border border-whiskey-100 bg-whiskey-50/60 p-4">
              <dt class="flex items-center gap-2">
                <svg class="h-4 w-4 text-whiskey-600" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" d="<%= icons[label] %>"></path>
                </svg>
                <span class="eyebrow text-whiskey-700"><%= label %></span>
              </dt>
              <dd class="mt-2 text-gray-700"><%= text %></dd>
            </div>
          <% end %>
        </dl>

        <% tags = @review.descriptor_tags %>
        <% if tags.any? %>
          <div class="mt-8 grid items-start gap-8 sm:grid-cols-[1fr_16rem]">
            <div data-controller="tag-picker">
              <p class="eyebrow text-gray-400">Flavor tags</p>
              <p class="mt-1 text-sm text-gray-500">Pulled from the words above. Pick one or several to find whiskeys that taste like this.</p>
              <div class="mt-3 flex flex-wrap gap-2">
                <% tags.each do |word, family| %>
                  <a href="<%= reviews_path(tags: word) %>" data-tag-picker-target="chip" data-tag="<%= word %>" data-on="0"
                     data-action="click->tag-picker#toggle" title="<%= family %> family"
                     class="rounded-full bg-whiskey-100 px-3 py-1 text-sm font-medium text-whiskey-800 transition hover:bg-whiskey-200"><%= word %></a>
                <% end %>
              </div>
              <a href="#" data-tag-picker-target="go"
                 class="mt-4 hidden inline-block font-semibold text-whiskey-700 hover:text-whiskey-600"></a>
            </div>
            <%= render "reviews/palate_wheel", review: @review %>
          </div>
        <% end %>
      </div>
    <% end %>

    <%= render "reviews/event_card", review: @review %>

    <div class="mt-10 flex items-center gap-4 border-t border-gray-100 pt-6">
      <%= link_to "All tastings of #{@review.bottle.name} →", bottle_path(@review.bottle), class: "font-semibold text-whiskey-700 hover:text-whiskey-600" %>
      <% if @review.user == current_user %>
        <%= link_to "Edit your review", edit_review_path(@review), class: "ml-auto rounded-xl bg-gray-100 px-4 py-2 text-sm font-semibold text-gray-700 transition hover:bg-gray-200" %>
      <% end %>
    </div>
  </div>
</section>
