<% content_for :title, "Dashboard - Admin" %>

<div class="mb-8">
  <h1 class="text-3xl font-bold text-gray-900">Dashboard</h1>
  <p class="mt-2 text-gray-600">Welcome to the Whiskey Share Society admin panel</p>
</div>

<!-- Stats Grid -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
  <!-- Total Users -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm font-medium text-gray-600">Total Users</p>
        <p class="text-3xl font-bold text-gray-900 mt-2"><%= @total_users %></p>
        <p class="text-xs text-gray-500 mt-1">
          <span class="text-green-600">+<%= @new_users_this_week %></span> this week
        </p>
      </div>
      <div class="p-3 bg-blue-50 rounded-xl">
        <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
        </svg>
      </div>
    </div>
  </div>

  <!-- Active Subscriptions -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm font-medium text-gray-600">Active Subscriptions</p>
        <p class="text-3xl font-bold text-gray-900 mt-2"><%= @active_subscriptions %></p>
        <p class="text-xs text-gray-500 mt-1">
          <%= number_to_percentage((@active_subscriptions.to_f / @total_users * 100), precision: 0) %> of users
        </p>
      </div>
      <div class="p-3 bg-green-50 rounded-xl">
        <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
      </div>
    </div>
  </div>

  <!-- Total Revenue -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm font-medium text-gray-600">Total Revenue</p>
        <p class="text-3xl font-bold text-gray-900 mt-2">$<%= number_with_precision(@total_revenue, precision: 2) %></p>
        <p class="text-xs text-gray-500 mt-1">
          $<%= number_with_precision(@revenue_this_month, precision: 2) %> this month
        </p>
      </div>
      <div class="p-3 bg-yellow-50 rounded-xl">
        <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
      </div>
    </div>
  </div>

  <!-- Total Presentations -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm font-medium text-gray-600">Presentations</p>
        <p class="text-3xl font-bold text-gray-900 mt-2"><%= @published_presentations %></p>
        <p class="text-xs text-gray-500 mt-1">
          <%= @total_presentations %> total, <%= @total_purchases %> purchases
        </p>
      </div>
      <div class="p-3 bg-purple-50 rounded-xl">
        <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"></path>
        </svg>
      </div>
    </div>
  </div>
</div>

<!-- Charts and Recent Activity -->
<div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
  <!-- Recent Signups Chart -->
  <div class="lg:col-span-2">
    <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
      <h2 class="text-lg font-semibold text-gray-900 mb-4">Recent Signups</h2>
      <div class="h-64 flex items-end justify-between space-x-2">
        <% max_count = @signup_chart_data.map { |d| d[:count] }.max || 1 %>
        <% @signup_chart_data.each do |data| %>
          <div class="flex-1 flex flex-col items-center">
            <div class="w-full bg-blue-500 rounded-t" 
                 style="height: <%= (data[:count].to_f / max_count * 100).to_i %>%;
                        min-height: <%= data[:count] > 0 ? '20px' : '2px' %>">
            </div>
            <span class="text-xs text-gray-500 mt-2"><%= data[:date] %></span>
            <span class="text-xs font-medium text-gray-700"><%= data[:count] %></span>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">Quick Actions</h2>
    <div class="space-y-3">
      <%= link_to new_admin_presentation_path, class: "flex items-center gap-3 p-3 rounded-lg hover:bg-gray-50 transition-colors" do %>
        <div class="p-2 bg-indigo-100 rounded-lg">
          <svg class="w-5 h-5 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-900">Add New Presentation</span>
      <% end %>
      
      <%= link_to admin_users_path, class: "flex items-center gap-3 p-3 rounded-lg hover:bg-gray-50 transition-colors" do %>
        <div class="p-2 bg-blue-100 rounded-lg">
          <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-900">View All Users</span>
      <% end %>
      
      <%= link_to admin_presentations_path, class: "flex items-center gap-3 p-3 rounded-lg hover:bg-gray-50 transition-colors" do %>
        <div class="p-2 bg-purple-100 rounded-lg">
          <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
          </svg>
        </div>
        <span class="text-sm font-medium text-gray-900">Manage Presentations</span>
      <% end %>
    </div>
  </div>
</div>

<!-- Recent Activity -->
<div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mt-8">
  <!-- Recent Users -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Recent Users</h2>
      <%= link_to "View All", admin_users_path, class: "text-sm text-indigo-600 hover:text-indigo-900" %>
    </div>
    <% if @recent_users.any? %>
      <ul class="space-y-3">
        <% @recent_users.each do |user| %>
          <li>
            <%= link_to admin_user_path(user), class: "flex items-center gap-3 p-2 rounded-lg hover:bg-gray-50 transition-colors" do %>
              <% if user.profile_image.attached? %>
                <%= image_tag user.profile_image, class: "h-10 w-10 rounded-full object-cover" %>
              <% else %>
                <div class="h-10 w-10 rounded-full flex items-center justify-center text-white font-semibold text-sm" style="background-color: <%= user.avatar_color %>">
                  <%= user.initials %>
                </div>
              <% end %>
              <div class="flex-1">
                <p class="text-sm font-medium text-gray-900"><%= user.full_name %></p>
                <p class="text-xs text-gray-500"><%= user.email %></p>
              </div>
              <span class="text-xs text-gray-500"><%= time_ago_in_words(user.created_at) %> ago</span>
            <% end %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-sm text-gray-500">No recent users</p>
    <% end %>
  </div>

  <!-- Recent Purchases -->
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Recent Purchases</h2>
      <%= link_to "View All", admin_presentations_path, class: "text-sm text-indigo-600 hover:text-indigo-900" %>
    </div>
    <% if @recent_purchases.any? %>
      <ul class="space-y-3">
        <% @recent_purchases.each do |purchase| %>
          <li class="flex items-center justify-between p-2">
            <div class="flex-1">
              <p class="text-sm font-medium text-gray-900"><%= purchase.presentation.title %></p>
              <p class="text-xs text-gray-500">by <%= purchase.user.full_name %></p>
            </div>
            <div class="text-right">
              <% if purchase.purchase_type == 'direct' %>
                <p class="text-sm font-medium text-gray-900">$<%= purchase.purchase_price || purchase.presentation.price %></p>
              <% else %>
                <p class="text-sm font-medium text-yellow-600">1 Credit</p>
              <% end %>
              <p class="text-xs text-gray-500"><%= time_ago_in_words(purchase.created_at) %> ago</p>
            </div>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-sm text-gray-500">No recent purchases</p>
    <% end %>
  </div>
</div>

<!-- Popular Presentations -->
<div class="mt-8">
  <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Popular Presentations</h2>
      <%= link_to "View All", admin_presentations_path, class: "text-sm text-indigo-600 hover:text-indigo-900" %>
    </div>
    <% if @popular_presentations.any? %>
      <div class="overflow-x-auto">
        <table class="min-w-full">
          <thead>
            <tr class="text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
              <th class="pb-3">Presentation</th>
              <th class="pb-3">Category</th>
              <th class="pb-3">Purchases</th>
              <th class="pb-3">Revenue</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
            <% @popular_presentations.each do |presentation| %>
              <tr>
                <td class="py-3">
                  <%= link_to admin_presentation_path(presentation), class: "text-sm font-medium text-gray-900 hover:text-indigo-600" do %>
                    <%= presentation.title %>
                  <% end %>
                </td>
                <td class="py-3">
                  <span class="text-sm text-gray-500"><%= presentation.category || 'Uncategorized' %></span>
                </td>
                <td class="py-3">
                  <span class="text-sm text-gray-900"><%= presentation.user_presentations.count %></span>
                </td>
                <td class="py-3">
                  <span class="text-sm text-gray-900">
                    $<%= presentation.user_presentations.where(purchase_type: 'direct').sum(:purchase_price) || 0 %>
                  </span>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-sm text-gray-500">No presentations yet</p>
    <% end %>
  </div>
</div>